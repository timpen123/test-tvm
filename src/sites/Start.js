import React from 'react';
import UserGrid from '../components/UserGrid';

/* This is the start screen that display the grid with all of the user and holds most of the functions */

const Start = () => {
  return (
    <div>
     <UserGrid/>
    </div>
  );
};

export default Start;