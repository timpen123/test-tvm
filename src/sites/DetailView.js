import React, { } from 'react';
import './DetailView.css';

/* This is the view that holds a detailed user if you press on a specifik user */

const DetailView = () => {
  return (
    <div>
     <DetailView/>
    </div>
  );
};

export default DetailView;